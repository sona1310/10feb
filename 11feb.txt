//print student name and subject whose age is between 10-20 years and name start with'A'.

start

for 1 t0 200 stds
{
 if((name.charAt[0]=='A') && (age>=10 && <=20))
    print stds.name and stds.sub
}

stop

//print student name whose city is either mumbai,chennai,and bihar.

start

for 1 to 200 stds
{
 if(stds.city=="mumbai" or stds.city=="chennai" or stds.city=="bihar")
  print stds.name and std.sub
}

stop