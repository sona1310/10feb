// print a min and max num from given array: a=[4,3,8,10,12].

start
 input a[4,3,8,10,12]

  int min=0, max=0;

for i=1 to array.length
  if(a[min]>a[i])
   min=i

  if(a[max]<a[i])
   max=i

    print min [min] and max [max]

stop